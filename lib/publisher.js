// == Imports ===============================================================

const Connected = require('./connected');

// == Exported Class ========================================================

class Publisher extends Connected {
}

// == Exports ===============================================================

module.exports = Publisher;
