// == Imports ===============================================================

const Connected = require('./connected');

// == Exported Class ========================================================

class Subscriber extends Connected {
}

// == Exports ===============================================================

module.exports = Subscriber;
